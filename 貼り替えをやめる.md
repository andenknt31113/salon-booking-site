# Code.gs の貼り替えをやめる（clasp）

`gas/Code.gs` を直すたびに、ブラウザで Apps Script を開いて、全選択して、貼って、保存して、
デプロイし直す ── これを毎回やるのは無理があります。**1行で送れるようにします。**

Google 公式のコマンドライン道具 **clasp** を使います。無料です。

> **この作業はローカルの環境でやってください。** ログイン情報（アカウントの鍵）を
> 作業用のクラウド環境に置きたくないためです。

---

## 1回だけの準備（5分）

### ① Apps Script API を有効にする

https://script.google.com/home/usersettings を開いて、
**「Google Apps Script API」を「オン」**にします。**これを忘れると必ず失敗します。**

### ② ログインする

```
npx @google/clasp login
```

ブラウザが開くので、**いま Apps Script を持っているアカウント**で許可します。
ログイン情報は `~/.clasprc.json` に保存されます。**このファイルは鍵そのものです。
人に渡さない、コミットしない。**（`.gitignore` に入れてあります）

### ③ つながっているか確かめる

```
cd salon-booking-site
npx @google/clasp status
```

`gas/Code.gs` と `gas/appsscript.json` が「送る対象」として並べば成功です。

---

## ふだんの流れ

### コードを送るだけ（実行して確かめたいとき）

```
npx @google/clasp push
```

これで Apps Script のエディタの中身が入れ替わります。**ブラウザで貼る必要はありません。**
そのあとエディタで `メニューを掲載内容に入れ替える` などを実行できます。

> `clasp push` は**上書き**です。ブラウザ側で直接編集していると、その変更は消えます。
> **編集はリポジトリ側だけにしてください。**

### サイトにも反映する（受け口を新しくする）

送っただけでは、**サイトが使っている受け口は古いままです。**
ウェブアプリはデプロイした時点のコードで固定されるためです。

```
npx @google/clasp deployments        # デプロイの一覧とID（AKfycb… ）を出す
npx @google/clasp deploy -i <デプロイID> -d "説明"
```

**`-i` で既存のデプロイIDを指定するのが肝心です。** これを付けないと新しいデプロイが
作られ、**URL が変わって予約が届かなくなります。**

### まとめて

```
npx @google/clasp push && npx @google/clasp deploy -i <デプロイID> -d "$(git log -1 --pretty=%s)"
```

コミットの1行目が、そのままデプロイの説明になります。あとから「どのコードが動いているか」を
追えるので、これを勧めます。

---

## それでもブラウザが要る場面

| | 理由 |
| --- | --- |
| **初回の権限承認** | スプレッドシート・メール・カレンダーへのアクセス許可 |
| **スクリプトプロパティ**（管理パスワード） | `clasp` からは触れません |
| **実行ログを読む** | `clasp logs` でも見られますが、画面のほうが読みやすいです |

---

## さらに自動にするなら（GitHub Actions）

`main` に push したら勝手に Apps Script まで反映させることもできます。
`~/.clasprc.json` の中身を GitHub の Secrets に入れて、CI から `clasp push` を実行する形です。

**ただし、これはアカウントの鍵を GitHub に預けることになります。** 便利ですが、

- いまの Apps Script は**作業者の個人アカウント**にあります
- **オーナーさんのアカウントに移したあと、そこへ同じことをするのは勧めません**
  （店主の Google アカウントの鍵を、作業者のリポジトリに置くことになります）

なので、**手元から `clasp push` を打つところまでで十分**だと考えています。
1行で済むなら、それ以上自動にする理由がありません。

---

## つまずいたとき

| 症状 | 原因 |
| --- | --- |
| `User has not enabled the Apps Script API` | ①を飛ばしています |
| `Invalid credentials` / 401 | `clasp login` をやり直してください |
| push したのにサイトが変わらない | **デプロイし直していません。**上の「サイトにも反映する」 |
| URL が変わってしまった | `-i <デプロイID>` を付けずにデプロイしました。古い方のIDで deploy し直せば戻ります |
| ブラウザで書いた変更が消えた | `clasp push` は上書きです。編集はリポジトリ側だけに |

---

## ★ `clasp pull` を使うときの落とし穴（2026-08-17 に踏みました）

**1. マニフェストの上書きに `y` と答えないこと**

`clasp push` の最初に

```
Manifest file has been updated. Do you want to push and overwrite? (y/N)
```

と聞かれたら、**N** です。向こうのマニフェストには、こちらに無い

```json
"webapp": { "executeAs": "USER_DEPLOYING", "access": "ANYONE_ANONYMOUS" }
```

が入っています。**`ANYONE_ANONYMOUS` は「アクセスできるユーザー＝全員」**で、
これが消えると**お客様から予約が届かなくなります**（DECISIONS.md）。
いまは `gas/appsscript.json` に取り込んであるので、もう聞かれないはずです。
それでも聞かれたら、`clasp pull` して差分を見てから決めてください。

**2. `clasp pull` は `gas/コード.js` を作ります。押し戻す前に必ず消すこと**

向こうのファイル名は `コード`（Apps Script の既定の名前）です。pull すると
`gas/コード.js` として落ちてきます。**これを置いたまま push すると、
プロジェクトに `Code` と `コード` が両方入り、同じ関数が二重に定義されて
スクリプトが丸ごと動かなくなります。**

```
rm gas/コード.js
```

`gas/Code.gs` は git に入っているので、pull で上書きされても
`git checkout gas/Code.gs` で戻せます。
