# 公開前チェックリスト

サイト自体は完成しています。公開して実際に予約を受けるまでに必要な作業です。
**上から順にやれば動きます。**

---

## A. サイトを見えるようにする（5分）

- [ ] リポジトリの **Settings → Pages** を開く
- [ ] **Source** を `Deploy from a branch`
- [ ] Branch を **`main`** / **`/(root)`** → **Save**
- [ ] 1分ほど待って https://andenknt31113.github.io/salon-booking-site/ が開くことを確認

この時点では、**予約しても店舗には届きません**（この端末に保存されるだけ）。
見た目の確認まではここでできます。

---

## B. 予約が届くようにする（30分）

詳しい手順は [README.md](README.md) の「実運用モード」にあります。

- [ ] **お店の名義で** Googleアカウントを作る（[アカウント準備.md](アカウント準備.md) 参照）
- [ ] スプレッドシートを新規作成
- [ ] 拡張機能 → Apps Script に [`gas/Code.gs`](gas/Code.gs) を貼る
- [ ] 先頭の `NOTIFY_EMAIL` を通知を受け取りたいアドレスに変更
- [ ] `testReserve` を実行して、シートに1行入る＋メールが届くことを確認
- [ ] デプロイ → ウェブアプリ → **アクセスできるユーザーを「全員」**
- [ ] 発行されたURLを `assets/js/data.js` の `reservationEndpoint` に貼る
- [ ] 実際にサイトから1件予約して、シートに入ることを確認
- [ ] テスト行を削除

**「アクセスできるユーザー」を「全員」にしないと予約が届きません。** ここだけ注意。

---

## C. 内容を実際のものにする

### 必須

- [ ] **通常メニューの一覧**（現在は「メンズカット ¥4,000」1件のみ）
- [ ] **各メニューの所要時間** ← これが空席カレンダーの押さえ時間になります
- [ ] 事業者名・代表者名・お問い合わせ先（`data.js` の `operator` / `operatorName` / `contactEmail`）
- [ ] プライバシーポリシーの制定日（`privacyUpdated`）
- [ ] **管理ページのパスワードを設定**
      Apps Script の「プロジェクトの設定 → スクリプト プロパティ」で
      `ADMIN_PASSWORD` を登録してください。サイトのソースには現れません

### 分かれば

- [ ] 電話番号（`tel`）→ 入れると電話ボタンが自動で出ます
- [ ] スタイル写真（現在はグラデーションのプレースホルダー）

### 全部済んだら

- [ ] `data.js` の `draft` を `false` に → 画面上部の「準備中」の帯が消えます

> **所要時間は特に重要です。**
> 長すぎると枠が埋まりすぎ、短すぎると次のお客様と重なります。

---

## D. 運用に入る（任意・効果が高い順）

- [ ] **Googleカレンダー連携** — `CALENDAR_ID` に `primary` を設定。
      スマホのカレンダーに予約が並び、通知も鳴ります
- [ ] **管理ページを使えるようにする** — `setupMenuSheets` を1回実行。
      「メニュー」「クーポン」「休業日」「設定」の4枚ができます。
      以降は `admin.html` から、または直接シートから編集できます
- [ ] **前日リマインド** — トリガーで `sendReminders` を毎日実行
- [ ] **Googleビジネスプロフィール** — 「龍ケ崎 バーバー」で探す人向け。
      口コミもここに集めるのが有利です
- [ ] **LINE公式アカウント** — お店の名義で開設し、作業者を管理者に招待

### LINEの通数について

無料プランは月200通です。**プッシュ配信のみが消費**し、
1:1トークの返信・自動応答・リッチメニューは消費しません。

`LINE_TOKEN` を設定すると予約1件ごとに1通消費するため、
**店舗への通知はメール／カレンダーに任せ、200通はお客様への配信に残す**
使い方をおすすめします。

---

## E. 動作確認（公開後）

実機で一度通してみてください。

- [ ] スマホでトップが開く
- [ ] クーポンを選んで予約が完了する
- [ ] 予約確認メールが届く（GAS設置後）
- [ ] スプレッドシートに行が入る
- [ ] 別の端末で「予約番号＋電話番号」で照会できる
- [ ] キャンセルすると枠が戻る
- [ ] LINEにURLを貼るとカードで表示される

---

## すでに確認済みのこと（開発側でテスト済み）

- 全9ページ、スマホ幅（390px）で横スクロールなし
- 予約フロー（メニュー→担当→日時→情報→確認→完了）
- ダブルブッキング防止（別端末の予約が × になる／確認画面滞在中の横取りを検知）
- キャンセルで枠が戻る
- 予約番号＋電話番号での照会（誤った電話番号では見えない）
- スプレッドシート由来のメニュー反映・「表示×」の除外
- localStorage が使えない環境でも動作
- JSエラー0件

---

## ロゴ・写真の差し込み（いただいたもの）

**リポジトリへのファイル追加だけ、こちらでは行えません。**
以下のファイル名で追加していただければ、そのまま反映されます。

| ファイル名 | 内容 | 反映される場所 |
| --- | --- | --- |
| `assets/logo.svg`（または `.png`） | ロゴ | ヘッダー・フッター・ヒーロー |
| `assets/staff-matteo.jpg` | オーナー様の写真 | スタッフ紹介・TOP |

追加したあと、`assets/js/data.js` の次の2箇所を変えてください。

```js
logo: 'assets/logo.svg',      // コメントアウトを外す
```

```js
image: 'assets/staff-matteo.jpg',   // staff の中
```

スタイル写真も同様に、`styles` の各 `image` にパスを入れれば置き換わります。

> ロゴはクリーム地に黒の意匠のため、**背景が透明なPNGかSVG**だと
> どのページでもきれいに収まります。
