# サロン予約サイト（店舗専用）

ホットペッパービューティーと同じ情報構成・予約フローを、
自社サロン1店舗専用のサイトとして実装したものです。
ビルド不要・依存パッケージなし。HTML / CSS / 素の JavaScript のみで動きます。

## 公開URL

https://andenknt31113.github.io/salon-booking-site/

### 初回だけ必要な設定

GitHub Pages の有効化は、リポジトリ所有者が画面から1回だけ行う必要があります
（外部ツールからは権限の都合で有効化できません）。

1. リポジトリの **Settings** → 左メニューの **Pages** を開く
2. **Source** を `Deploy from a branch` にする
3. Branch を **`main`** / **`/(root)`** にして **Save**

1分ほどで上記URLが開くようになります。以降は `main` に push するだけで自動的に反映されます。

## ページ構成

| ファイル | 内容 |
| --- | --- |
| `index.html` | サロンTOP（クーポン・スタイル・スタッフ・口コミ・店舗情報・FAQ） |
| `gallery.html` | ヘアスタイル一覧（長さ別の絞り込み） |
| `staff.html` | スタッフ紹介（指名予約への導線つき） |
| `menu.html` | クーポン・通常メニュー（カテゴリ別タブ） |
| `reviews.html` | 口コミ一覧 |
| `reserve.html` | **空席カレンダー ＋ 予約フロー（STEP1〜5）** |
| `mypage.html` | ご予約の確認・キャンセル（お客様向け） |
| `admin.html` | 予約管理（スタッフ用・パスコード式） |

| スクリプト | 役割 |
| --- | --- |
| `assets/js/data.js` | **サロン設定（ここだけ書き換えれば自社用になります）** |
| `assets/js/common.js` | ヘッダー/フッター・共通関数・予約データ・空席計算・受信先への送信 |
| `assets/js/pages.js` | 一覧系ページの描画 |
| `assets/js/reserve.js` | 予約フローの制御 |
| `assets/js/mypage.js` | 予約確認・キャンセル |
| `assets/js/admin.js` | 予約管理・CSV出力 |
| `gas/Code.gs` | Google Apps Script（予約の受信先） |

## 予約フロー

ホットペッパービューティーと同じ順序です。

```
STEP1 メニュー → STEP2 スタッフ → STEP3 日時 → STEP4 お客様情報 → STEP5 確認 → 完了
```

STEP3 の空席カレンダーは「時間 × 日付」のグリッドで、
選択中メニューの所要時間ぶん連続して空いている枠だけを予約可として表示します。

| 記号 | 意味 |
| --- | --- |
| ◎ | 空きに余裕あり |
| ○ | 予約可 |
| △ | 残りわずか |
| × | 予約不可 |
| - | 休業日 |

画面下部に「電話」「24時間ネット予約」を固定表示しているので、スマホからすぐ予約に進めます。

## 自社用にする

編集するのは基本的に **`assets/js/data.js` の1ファイルだけ**です。

```js
name: 'Beauty Salon LUMIÈRE',   // サロン名
branch: '表参道店',
tel: '03-1234-5678',
address: '東京都渋谷区神宮前0-0-0',

business: {
  openTime: '10:00',      // 営業開始
  closeTime: '20:00',     // 営業終了
  lastOrder: '19:00',     // 最終受付
  slotMinutes: 30,        // 予約枠の刻み
  closedWeekdays: [2],    // 定休日（0=日 … 6=土）
  closedDates: [],        // 臨時休業日 '2026-08-20'
  bookableDays: 60,       // 何日先まで予約を受けるか
  minLeadHours: 2,        // 当日予約の締め切り
  calendarDays: 14        // 空席カレンダーの表示日数
}
```

`coupons` / `menuCategories` / `staff` / `styles` / `reviews` / `faq` の配列を
書き換え・追加すると、各ページと予約フローの選択肢に自動で反映されます。
スタッフの `workdays`（出勤曜日）と `nominationFee`（指名料）は
空席カレンダーと料金計算にそのまま使われます。

配色を変えたいときは `assets/css/style.css` の先頭にある `--accent` を書き換えてください。

## 予約データの扱い（重要）

GitHub Pages はサーバー処理を持たない静的ホスティングです。そのため2つのモードがあります。

### デモモード（初期状態）

予約はその端末のブラウザ（localStorage）に保存されるだけで、**店舗側には届きません。**
動作確認・社内でのレビュー用です。

### 実運用モード（スプレッドシートを予約台帳にする）

予約を Google スプレッドシートに集めます。**スプレッドシートがそのまま管理画面になり**、
スマホの Google スプレッドシートアプリから予約一覧を確認・編集できます。

構成はこうなります。

```
お客様のスマホ  ──予約──▶  Google Apps Script  ──▶  スプレッドシート（予約台帳）
                                    └──▶  店舗にメール通知
```

#### 手順

**1. スプレッドシートを作る**

Google ドライブで新しいスプレッドシートを作成します。名前は「予約台帳」など任意で構いません。

**2. スクリプトを貼る**

そのスプレッドシートで **拡張機能 → Apps Script** を開き、
最初から入っているコードをすべて消して、このリポジトリの
[`gas/Code.gs`](gas/Code.gs) の中身を貼り付けます。

先頭の設定を自分のものに変えてください。

```js
const NOTIFY_EMAIL = 'salon@example.com';       // 通知を受け取るメールアドレス
const SALON_NAME   = 'Salon LUMIÈRE 表参道店';  // メール件名の先頭
```

**3. 先に動作確認する**

エディタ上部の関数選択で `testReserve` を選んで実行します。
初回は権限の確認画面が出るので許可してください
（「このアプリは確認されていません」と出たら、詳細 → 安全ではないページに移動）。

スプレッドシートに見出し行とテスト予約が1件入り、メールが届けば成功です。
確認できたらテスト行は削除して構いません。

**4. ウェブアプリとして公開する**

**デプロイ → 新しいデプロイ → 種類の選択で「ウェブアプリ」** を選び、

| 項目 | 設定 |
| --- | --- |
| 次のユーザーとして実行 | 自分 |
| アクセスできるユーザー | **全員** |

「アクセスできるユーザー」を**全員**にしないと、お客様からの予約が届きません。
ここはスプレッドシート自体が公開されるわけではなく、
受け口だけが公開される設定なので、台帳の中身は他人には見えません。

発行された `https://script.google.com/macros/s/.../exec` をコピーします。

**5. サイトにURLを貼る**

`assets/app.js` の `endpoint` に貼り付けて push すれば完了です。

```js
endpoint: 'https://script.google.com/macros/s/xxxxx/exec',
```

#### 届くもの

| タイミング | スプレッドシート | メール |
| --- | --- | --- |
| 予約が入ったとき | 1行追加（状態＝予約確定） | 【新規予約】として通知 |
| お客様がキャンセルしたとき | その行の状態を「キャンセル」に変更＋取り消し線 | 【キャンセル】として通知 |

台帳の列は 予約番号 / 受付日時 / 来店日 / 開始 / 終了 / メニュー / 担当 / 指名料 /
合計金額 / 所要(分) / お名前 / 電話番号 / ご要望 / 状態 です。

#### 補足

送信時の `Content-Type` を `text/plain` にしているのは意図的です。
`application/json` にするとブラウザが事前確認（OPTIONS リクエスト）を送りますが、
Apps Script はこれに応答できないため、送信が必ず失敗します。

## 空席状況について

初期状態の空席カレンダーはサンプルです。次のルールで組み立てています。

- 定休日・臨時休業日・過去の時間・受付締切（`minLeadHours`）を除外
- スタッフの出勤曜日（`workdays`）を反映
- 1日に1〜3件の先約が入っている想定でブロックを生成
- 選択中メニューの合計所要時間ぶん、連続して空いている枠だけを予約可に
- この端末から入れた予約は、その担当の枠を実際に埋める

実際の予約管理システムとつなぐときは、`assets/js/common.js` の
`Availability.isStaffSlotFree()` を自社の空き状況を返す処理に置き換えてください。
ここ1箇所の変更で全画面に反映されます。

> **既知の制約：ダブルブッキング**
> 空席の判定はお客様の端末ごとに独立しているため、別々の端末から
> 同じ枠を予約できてしまいます。解消するには、空席状況も受信先（GAS）から
> 読み込む必要があります。

## 管理画面について

`admin.html` から、そのブラウザに入っている予約の一覧・絞り込み・CSV出力・
キャンセルができます。入口のパスコードは `data.js` の `adminPasscode` です。

ただし**日常の予約確認は、GAS で連携したスプレッドシートを見るのが確実です。**
`admin.html` はそのブラウザに保存された予約しか表示できないため、
お客様の端末から入った予約は表示されません。

> **注意:** パスコードはソースを読めば分かります。
> 本番で顧客情報を扱う場合は、必ずサーバー側の認証を用意してください。

## ローカルで見る

```bash
python3 -m http.server 8000
# → http://localhost:8000
```
